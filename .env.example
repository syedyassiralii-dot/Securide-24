# NewsData.io API key — set this in your Render environment variables dashboard
# Do NOT commit a real key to version control
NEWSDATA_API_KEY=your_newsdata_api_key_here

# Port (Render sets this automatically; leave unset locally to use 3000)
PORT=3000
